# Versioning

Current release version:

```text
1.0.0+12
```

Meaning:

- `1.0.0` = public version name
- `12` = build number / version code for Phase 12 release preparation

Update in `pubspec.yaml` before every store upload.
