# Phase 36I validation note

The Developer production-art branch intentionally keeps legacy `char_developer_*` keys for scenario compatibility while routing their presentation to authored Lottie assets. Final acceptance requires analyzer, full tests, web release, Android APK, and both shareable artifact uploads on the same PR head.
